BASE_IMAGE_NAME="ubuntu" # ベースとなるIMAGEの名前
BASE_IMAGE_TAG="16.04" # ベースとなるIMAGEのタグ
BUILD_IMAGE_NAME="apache-perl-dev-image" # ビルドされるIMAGEの名前（ユニークなものをつける）
CONTAINER_NAME="apache-perl-dev-container"
WORK_DIR="" # マウントしたいディレクトリの絶対パス（なにも入力しない場合、今いるディレクトリをマウントします）# /work にマウントします
WORK_DIR_2nd="" # 動作させたいWebソースがあるディレクトリ**今回は /var/www/html にマウントします**
WORK_DIR_3rd="" # ３つめのディレクトリ（なければ空欄に）# /work_3rd にマウントします
USE_GPU=0 # GPUありなら1、そうでなければ0
PORT="80" # ポートを使うならポート番号（使わないなら空欄）-> **今回は80に転送します**
BACKGROUND=1 # バックグラウンド実行なら1、そうでなければ0
USER_SWITH=0 # イメージの作成者とコンテナの立ち上げるユーザーアカウントが違う場合は1（基本的には0）